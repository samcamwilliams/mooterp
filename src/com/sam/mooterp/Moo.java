package com.sam.mooterp;

import org.antlr.v4.runtime.CharStreams;
import org.antlr.v4.runtime.CodePointCharStream;
import org.antlr.v4.runtime.CommonTokenStream;
import org.antlr.v4.runtime.TokenStream;
import org.jetbrains.annotations.NotNull;

/**
 * Created by spine on 22/06/17.
 */
public class Moo {

  /*public ResolveParser getParser(@NotNull String lessonContent) {
    CodePointCharStream input = CharStreams.fromString(lessonContent);
    ResolveLexer lexer = new ResolveLexer(input);
    TokenStream tokens = new CommonTokenStream(lexer);
    ResolveParser parser = new ResolveParser(tokens);
    parser.removeErrorListeners();
    parser.addErrorListener(errorManager);
    return parser;
  }*/

}
